# 🚀 Деплой RentalAll через GitHub

## 📋 **Часть 1: Загрузка проекта на GitHub (на локальном ПК)**

### **Шаг 1: Инициализация Git**

Откройте PowerShell в папке проекта:

```powershell
cd E:\Study\Диплом\rentalall
```

Инициализируйте Git (если еще не инициализирован):

```powershell
git init
```

Добавьте все файлы:

```powershell
git add .
```

Сделайте первый коммит:

```powershell
git commit -m "Initial commit: RentalAll project"
```

---

### **Шаг 2: Создание репозитория на GitHub**

1. **Зайдите на GitHub**: https://github.com
2. **Войдите** в свой аккаунт (или создайте новый)
3. Нажмите **"+" → "New repository"** (справа вверху)
4. Заполните:
   - **Repository name**: `rentalall`
   - **Description**: `Платформа для бронирования площадок - дипломный проект`
   - **Visibility**: 
     - ✅ **Private** (рекомендуется, если не хотите показывать код всем)
     - ⚠️ **Public** (если хотите открытый проект)
   - ❌ **НЕ ставьте галочки** на "Add README", "Add .gitignore", "Choose a license" (у нас уже есть)
5. Нажмите **"Create repository"**

---

### **Шаг 3: Связывание с GitHub и push**

GitHub покажет инструкции, но вот что нужно сделать:

```powershell
# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/rentalall.git

# Переименуйте ветку в main (если у вас master)
git branch -M main

# Запушьте код на GitHub
git push -u origin main
```

**Пример:**
Если ваш GitHub username — `ivanov123`, то команда будет:
```powershell
git remote add origin https://github.com/ivanov123/rentalall.git
```

При первом push GitHub попросит авторизоваться:
- **Username**: ваш GitHub username
- **Password**: используйте **Personal Access Token** (не обычный пароль!)

---

### **📝 Как создать Personal Access Token (PAT):**

1. GitHub → **Settings** (ваш профиль справа вверху)
2. **Developer settings** (внизу слева)
3. **Personal access tokens** → **Tokens (classic)**
4. **Generate new token** → **Generate new token (classic)**
5. Заполните:
   - **Note**: `RentalAll Deploy`
   - **Expiration**: `90 days` (или больше)
   - **Select scopes**: поставьте галочку на **`repo`** (полный доступ к репозиториям)
6. **Generate token**
7. **Скопируйте токен** (он показывается только один раз!)
8. Используйте этот токен вместо пароля при git push

---

### **✅ Проверка:**

Обновите страницу репозитория на GitHub — вы должны увидеть все файлы проекта!

---

## 📦 **Часть 2: Деплой на сервер через Git**

### **Шаг 1: Подключение к серверу**

На вашем локальном ПК:

```powershell
ssh root@62.192.174.91
```

Пароль: `15Y02MR8QwQr`

---

### **Шаг 2: Клонирование проекта с GitHub**

На сервере:

```bash
cd /root

# Клонируйте репозиторий (замените YOUR_USERNAME!)
git clone https://github.com/YOUR_USERNAME/rentalall.git

# Перейдите в папку
cd rentalall
```

**Если репозиторий приватный**, GitHub попросит авторизацию:
- **Username**: ваш GitHub username
- **Password**: ваш Personal Access Token (тот же, что использовали для push)

---

### **Шаг 3: Настройка .env файла**

```bash
# Создайте .env из примера
cp deploy/env.production.example backend/.env

# Отредактируйте .env
nano backend/.env
```

**Обязательно заполните:**

1. **SECRET_KEY** — сгенерируйте новый:
   ```bash
   python3 -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
   ```
   Скопируйте результат в `SECRET_KEY=`

2. **DB_PASSWORD** — придумайте сложный пароль (например, `RentAll2024!Secure`)

3. **Остальные параметры** уже заполнены правильно

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

---

### **Шаг 4: Запуск автоматического деплоя**

```bash
# Сделайте скрипт исполняемым
chmod +x deploy/deploy.sh

# Запустите деплой
./deploy/deploy.sh
```

⏱️ **Процесс займет 5-10 минут.** Скрипт автоматически:
- Установит все зависимости
- Создаст PostgreSQL базу данных
- Применит Django миграции
- Соберет React production build
- Настроит Nginx и systemd
- Запустит сервисы

---

### **Шаг 5: Настройка DNS**

Пока деплой идет, настройте DNS в панели вашего регистратора доменов:

| Тип | Имя | Значение       | TTL  |
|-----|-----|----------------|------|
| A   | @   | 62.192.174.91  | 3600 |
| A   | www | 62.192.174.91  | 3600 |

Проверьте (может занять 15-30 минут):
```bash
ping rentalall.ru
```

Должен показывать: `62.192.174.91`

---

### **Шаг 6: Получение SSL сертификата**

После того, как DNS заработает:

```bash
certbot --nginx -d rentalall.ru -d www.rentalall.ru
```

Введите email → согласитесь с условиями.

```bash
systemctl restart nginx
```

---

### **Шаг 7: Проверка работы**

Откройте в браузере:
- 🌐 **Сайт**: https://rentalall.ru
- 🔧 **Админка**: https://rentalall.ru/admin (логин: `admin`, пароль: `admin123`)
- 📚 **API**: https://rentalall.ru/swagger

---

## 🔄 **Обновление проекта (после изменений в коде)**

### **На локальном ПК:**

После внесения изменений в код:

```powershell
cd E:\Study\Диплом\rentalall

# Добавьте измененные файлы
git add .

# Сделайте коммит с описанием изменений
git commit -m "Описание изменений"

# Запушьте на GitHub
git push origin main
```

---

### **На сервере:**

Обновите код на сервере:

```bash
cd /root/rentalall

# Получите последние изменения
git pull origin main

# Backend (если были изменения)
cd backend
source venv/bin/activate
pip install -r requirements.txt
python manage.py migrate
python manage.py collectstatic --noinput
deactivate

# Frontend (если были изменения)
cd ../frontend
npm install
npm run build

# Перезапустите сервисы
systemctl restart rentalall.service
systemctl restart nginx
```

---

## 🔐 **Безопасность (ОБЯЗАТЕЛЬНО!):**

### **1. Смените root-пароль:**
```bash
passwd
```

### **2. Смените Django admin пароль:**
- Зайдите на https://rentalall.ru/admin
- Нажмите "admin" справа вверху → "Change password"

### **3. Настройте firewall:**
```bash
apt install ufw
ufw allow 22,80,443/tcp
ufw enable
```

### **4. Настройте SSH-ключи (опционально, но рекомендуется):**

На вашем локальном ПК:

```powershell
# Генерация SSH ключа (если еще нет)
ssh-keygen -t ed25519 -C "your_email@example.com"

# Копирование ключа на сервер
type $env:USERPROFILE\.ssh\id_ed25519.pub | ssh root@62.192.174.91 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

Теперь можно подключаться без пароля:
```powershell
ssh root@62.192.174.91
```

---

## 🐛 **Troubleshooting**

### **Проблема: Git push требует пароль каждый раз**

**Решение:** Используйте SSH вместо HTTPS:

```powershell
# Измените URL репозитория
git remote set-url origin git@github.com:YOUR_USERNAME/rentalall.git
```

Но сначала настройте SSH-ключ на GitHub:
1. Генерируйте ключ: `ssh-keygen -t ed25519`
2. Скопируйте ключ: `type $env:USERPROFILE\.ssh\id_ed25519.pub`
3. GitHub → Settings → SSH and GPG keys → New SSH key
4. Вставьте ключ

---

### **Проблема: Django не запускается**

```bash
# Проверьте логи
journalctl -u rentalall.service -n 50

# Проверьте .env
cat /root/rentalall/backend/.env

# Проверьте БД
sudo -u postgres psql -l | grep rentalall
```

---

### **Проблема: Nginx 502 Bad Gateway**

```bash
# Проверьте статус Django
systemctl status rentalall.service

# Если не запущен:
systemctl start rentalall.service
```

---

## 📋 **Полезные команды**

### **Git:**
```bash
# Проверить статус
git status

# Посмотреть последние коммиты
git log --oneline -5

# Откатить последний коммит (на локальном ПК)
git reset --soft HEAD~1
```

### **Сервер:**
```bash
# Логи Django
journalctl -u rentalall.service -f

# Логи Nginx
tail -f /var/log/nginx/rentalall_error.log

# Перезапуск сервисов
systemctl restart rentalall.service
systemctl restart nginx

# Статус сервисов
systemctl status rentalall.service
systemctl status nginx
```

---

## ✅ **Готово!**

Ваш проект теперь:
- ✅ На GitHub (версионирование, резервное копирование)
- ✅ На сервере (production deployment)
- ✅ Доступен по адресу https://rentalall.ru

**Преимущества этого подхода:**
- 📦 Легко обновлять проект (`git pull`)
- 💾 Резервное копирование на GitHub
- 🔄 История изменений
- 👥 Возможность совместной разработки

---

## 📞 **Поддержка**

Подробные инструкции и troubleshooting: [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)

